#include "loadSettings.h"
void loadSettings() {
	std::vector<stripType> loadedTypes;
	loadedTypes.push_back(stripType());
	loadedTypes[0].stripTemplate(CImg<unsigned int>());
	for (int i = 0; i < 12; i++) {
		
	}
}